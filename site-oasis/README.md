# Winnow site, oasis draft

Second landing-page draft with the desert/oasis positioning; same static stack as `site/` (`index.html`, `styles.css`, `main.js`, no build). Deploy on Vercel with Root Directory = `site-oasis`, Framework Preset = Other.
